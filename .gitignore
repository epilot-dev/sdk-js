node_modules
npm_debug.log*

# This is a pnpm workspace (see "packageManager" in package.json).
# npm/yarn lockfiles are not used and must not be committed.
package-lock.json
yarn.lock
.eslintcache
dist
*.log
.idea
**/*.DS_Store
.prototools
.npmrc
.epilot-docs
.claude