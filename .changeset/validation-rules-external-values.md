---
"@epilot/validation-rules-client": minor
---

Add `ExternalValue` and `EnvironmentValue` comparison value sources to v2 validation rules (`source: external` resolved from an app's External Values hook; `source: environment` resolved from an organisation environment variable), also allowed as adjustment amounts and `applies_when` values.
