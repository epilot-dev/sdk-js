---
"@epilot/validation-rules-client": minor
---

Add `ExternalValue` comparison value source (`source: external`, `app_id`, `hook_id`, `result_id`) to v2 validation rules, resolved from an app's External Values hook.
