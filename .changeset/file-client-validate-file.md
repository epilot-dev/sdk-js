---
'@epilot/file-client': minor
'@epilot/sdk': minor
---

Add `validateFile` (`POST /v1/files/validations`): validate an uploaded file, referenced by file entity id or S3 reference, against document validation rules and receive per-rule, per-condition outcomes with the observed file facts
