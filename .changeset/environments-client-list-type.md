---
'@epilot/environments-client': minor
---

Add the `List` environment variable value type: an ordered collection of one declared element type (`Text`, `Number`, `Boolean`, `JSON` or `Link`), plus the `LinkFields` schema its `Link` items carry (ER-5355)
