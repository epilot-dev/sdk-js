---
"@epilot/configuration-hub-client": patch
"@epilot/sdk": patch
"@epilot/cli": patch
---

Refresh the Configuration Hub API contract and expose `cancelSyncJob`, which sends `POST /v1/configs/sync-jobs/{id}/cancel`.
