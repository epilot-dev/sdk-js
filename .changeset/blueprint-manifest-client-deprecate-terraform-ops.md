---
"@epilot/blueprint-manifest-client": minor
---

Mark the Terraform-based operations as `@deprecated` and point callers at the V3 operations. Also picks up `updateBlueprintNote`, which the API spec gained since the last regeneration.
