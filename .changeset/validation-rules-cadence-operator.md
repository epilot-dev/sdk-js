---
"@epilot/validation-rules-client": minor
"@epilot/sdk": minor
---

Refresh the Validation Rules API contract: adds the `maxChangesInPeriod` operator and the `CadenceValue` condition value that limits how often a context attribute may change within a period.
